# setting.txt (템플릿)
# 이 파일로 시스템/부하 출력 항목을 선택합니다.
# - 줄 시작이 # 이면 주석으로 무시됩니다.
# - 값은 콤마(,)로 구분합니다. 공백 없이 적는 것을 권장합니다.
# - 비워두면(또는 이 줄을 삭제하면) 해당 그룹의 모든 항목을 출력합니다.
#
# 선택 가능 항목 목록
#   info: os, os_version, cpu_model, cpu_core_count, memory_total_gb
#   load: cpu_usage_percent, memory_usage_percent
#
# 예시
#   info=os,os_version,cpu_model
#   load=cpu_usage_percent
#
# 아래 두 줄에서 ‘채워넣어야해’를 원하는 항목들로 바꾸세요(쉼표로 구분).
info=채워넣어야해
load=채워넣어야해
